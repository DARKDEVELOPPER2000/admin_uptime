// Plus tard: ici tu branches ton backend (REST / WebSocket)
// Exemple:
// export async function getActiveGarages(){ return fetch('/api/garages/active').then(r=>r.json()); }

export const api = {
  // mock placeholders
};
